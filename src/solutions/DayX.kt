package solutions

import Solver

class DayX(version: String) : Solver(0, version)
{
    override fun part1(): String
    {
        var result = 0
        return result.toString()
    }

    override fun part2(): String
    {
        var result = 0
        return result.toString()
    }
}